package edu.uoregon.cs.calendar499;
// This is a enum [a list of values that store some meaning to the user but not the system, which can interpret them in whatever way it wants] (public for visibility in other modules)
// It's purpose is to denote the status of the FileIOStatus object.
public enum Status {
	Waiting, Success, Failed;
}
